# KiCad temporary, backup and autosave files
*-backup
*-BAK
*-cache/
*-table
autosave-*
*.autosave
*.bak
*.lck
*.lock

# KiCad user/project settings
*.pro.user
mysettings.kicad_pro

# Common temporary/old project files
Version1.*
untitled.*

# 3D models (track with Git LFS instead if needed)
3d_models/*.step
3d_models/*.stl
